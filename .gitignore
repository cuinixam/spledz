build/
.venv/
__pycache__/
.bootstrap/

# west workspace: this repository is the workspace root, so the projects it
# manages are checked out here.
.west/
deps/
# Modules imported from Zephyr's manifest keep the path upstream gives them.
modules/
