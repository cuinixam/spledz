{
  "name": "SPLed on Zephyr",
  "image": "mcr.microsoft.com/devcontainers/base:debian",
  // Zephyr's variables are not set here: the ZephyrSetup pipeline step owns them
  // and they arrive by sourcing build/install/env_setup.sh.
  "containerEnv": {
    "LANG": "C.UTF-8",
    "LC_ALL": "C.UTF-8"
  },
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends cmake ninja-build gcc g++ python3 python3-venv device-tree-compiler pipx && pipx ensurepath && pipx install pypeline-runner",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "ms-python.python"
      ]
    }
  }
}
