# The board's LED is a WS2812 on a data line, not a GPIO that goes on and off,
# so it is driven through the led_strip API. The I2S driver behind it is
# selected by the devicetree node in the overlay next to this file.
CONFIG_LED_STRIP=y
