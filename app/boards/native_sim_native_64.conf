# Keep the shell in the terminal that started the binary. The LED display uses
# the second UART, which stays on its own pseudo terminal.
#
# Board files are merged on top of prj_<variant>.conf rather than replacing it,
# so this line lives here once instead of in every variant.
CONFIG_UART_NATIVE_PTY_0_ON_STDINOUT=y
